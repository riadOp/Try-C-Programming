//write a code to calculate the area of a square(side given)
#include<stdio.h>

int main(){
    int side;
    printf("Enter the mesure = ");
    scanf("%d" , &side );
    printf("Area of square is = %d " , side * side );
    return 0 ;
}